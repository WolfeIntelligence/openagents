schema: 1
name: data-cleaning
owner: openagents
version: 1.0.0
kind: skill
title: Dataset Profiler
summary: "Profile a messy dataset before analyzing it: types, missingness, duplicates, outliers, and the questions it cannot answer"
license: MIT
tags: [data, analysis, cleaning, pandas, skill]
runtimes: [claude-code, codex, openai-agents, generic]
pricing:
  model: free
  amount_cents: 0
  currency: usd
entry: SKILL.md
files:
  - SKILL.md
  - checks.md
inputs:
  - name: path
    type: string
    required: true
    description: "Path to the dataset (csv, tsv, parquet, json lines)"
  - name: target
    type: string
    required: false
    description: "The question the data is meant to answer, if known"
requires: []
