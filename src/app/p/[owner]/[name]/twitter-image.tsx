// Twitter Card image: identical to the Open Graph image (dark card, same
// content), so re-export rather than duplicate the ImageResponse JSX.
export { default, alt, size, contentType } from "./opengraph-image";
